# Raw Data Directory

This directory contains source data exactly as received (e.g. `customer_transactions.csv`, `sales_2024.xlsx`).

> **Rule:** Never modify or edit files in this folder directly. Raw data is sacred and serves as the single source of truth for all downstream analytics and data pipelines.
